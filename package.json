{
  "name": "logos-node-observer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Public, read-only health dashboard for operator-owned Logos nodes.",
  "scripts": {
    "test": "node --test",
    "check": "node --check web/app.js",
    "preview": "node scripts/preview-server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@netlify/blobs": "10.7.10"
  }
}
